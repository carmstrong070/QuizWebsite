@model UserDetailsViewModel
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center mb-5">
    <h1>Edit Account Information:</h1>
</div>

<div class="h-100 d-flex align-items-center justify-content-center">
    <div class="w-50">
        <form method="post">
            <div class="form-outline mb-4">
                @Html.TextBoxFor(m => m.Email, new { @type = "email", @class = "form-control" })
                <label class="form-label" for="form2Example1">Email address</label>
            </div>
            <div class="form-outline mb-4">
                @Html.TextBoxFor(m => m.Username, new { @class = "form-control" })
                <label class="form-label" for="form2Example1">User name</label>
            </div>
            <div class="form-outline mb-4">
                @Html.TextBoxFor(m => m.Password, new { @type = "password", @class = "form-control" })
                <label class="form-label" for="form2Example2">Password</label>
            </div>
            <div class="row mb-4">
                <button type="submit" class="btn btn-primary btn-block mb-4">Submit Changes</button>
            </div>
        </form>
    </div>
</div>